{
  "name": "root",
  "private": true,
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "lint": "lerna run lint",
    "build": "lerna run build",
    "prepare": "husky install"
  },
  "devDependencies": {
    "husky": "^7.0.0",
    "lerna": "^4.0.0"
  },
  "dependencies": {
    "package-json-type": "^1.0.3"
  }
}
