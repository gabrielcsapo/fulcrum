{
  "name": "root",
  "private": true,
  "scripts": {
    "lint": "lerna run lint",
    "build": "lerna run build",
    "prepare": "husky install"
  },
  "devDependencies": {
    "husky": "^7.0.0",
    "lerna": "^4.0.0"
  }
}
