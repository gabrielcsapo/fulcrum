{
  "name": "root",
  "private": true,
  "scripts": {
    "lint": "npm run lint --workspaces",
    "build": "npm run build --workspaces",
    "docs": "npm run build && ./packages/module-detective/bin/index.js --report --outputDir docs",
    "prepare": "husky install"
  },
  "devDependencies": {
    "husky": "^7.0.0"
  },
  "workspaces": ["packages/*"]
}
