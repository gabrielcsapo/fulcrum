{
  "name": "root",
  "private": true,
  "scripts": {
    "lint": "npm run lint --workspaces",
    "build": "npm run build --workspaces",
    "example-report": "npm run build && ./packages/package-inspector/bin/index.js --report --outputDir report",
    "prepare": "husky install"
  },
  "devDependencies": {
    "husky": "^7.0.0"
  },
  "workspaces": ["packages/*"]
}
