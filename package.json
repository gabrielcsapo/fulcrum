{
  "name": "root",
  "private": true,
  "scripts": {
    "lint": "npm run lint --workspaces",
    "build": "npm run build --workspaces",
    "prepare": "husky install"
  },
  "devDependencies": {
    "husky": "^7.0.0"
  },
  "workspaces": ["packages/*"]
}
